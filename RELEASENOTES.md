# EnvMatch 版本迭代日志 (Release Notes)

## [v1.4.1] - 2026-05-12

### 🧠 智能审计报文解析 (Smart JSON Auditing)
- **递归式转义 JSON 解析**:
  - 引入 `autoParseJson` 算法，支持对嵌套在字符串内部的、多层转义的 JSON 内容进行自动解包与格式化。
  - **智能片段提取 (Smart Extraction)**: 即使 JSON 块嵌入在长篇 Prompt 或 LLM 回复文本中，系统也能通过括号匹配算法自动提取、解析并以结构化方式展示。
  - **Markdown 代码块适配**: 自动识别并剥离 ` ```json ` 等 Markdown 标识符，实现纯净的结构化展示。
- **报文查看器交互优化**:
  - **精准拖拽与文本选中**: 为全屏 JSON 查看器引入专用的“头部拖拽句柄”，彻底解决了窗口拖拽与鼠标划选文本之间的交互冲突，支持快速复制报文片段。
  - **双重视图模式**: 
    - **详情页 (Raw)**: 展示原始报文，确保审计数据的真实性与完整性。
    - **全屏页 (Smart)**: 开启智能解析，将复杂的转义字符串转化为易读的 JSON 树状结构。

### 🎨 交互式流程架构图 (Interactive Pipeline)
- **分析流程可视化升级**:
  - 首页工作台的“分析流程”架构图现已支持全方位交互。
  - **分层深度介绍**: 点击“输入层、处理层、模型层、输出层、应用层”任意图标，即可弹出对应的玻璃态详情窗。
  - **全链路技术揭秘**: 弹窗详细展示了每一层级的层级概述、核心功能点以及底层技术栈（如 OpenCV, Scenedetect, VLM 路由等）。
  - **UI 动效加强**: 增加了层级图标旋转缩放动效及平滑的 `framer-motion` 弹窗过渡。

---

## [v1.4.0] - 2026-05-11

### 🚀 原生视频识别与审计体系 (Native Video & Auditing)
- **原生视频分析链路 (DashScope SDK)**:
  - 深度集成 **Alibaba DashScope SDK**，实现 Qwen-VL-Plus 等模型的高性能原生视频流直传。
  - 弃用传统的网格缝合方案作为视频模式的后端，利用模型原生时序感知提升分析准确度。
  - **物理级视频预处理**: 新增 FFmpeg 物理压制管线，支持根据任务配置实时生成 360p-1080p 规格视频，平衡识别效果与 Token 成本。
- **全方位模型调用审计 (Model Auditing)**:
  - 新增 **“模型调用记录”** 管理模块，端到端追踪每次 API 请求的原始 Payload、响应报文及网络时延。
  - **交互式报文分析器**:
    - 支持 JSON 报文的 **单行省略展示** 与 **Hover 全屏气泡**。
    - 引入 **高级窗口化查看器**: 支持 JSON 内容的全屏弹窗、自由拖拽、动态缩放及一键复制。
- **Qwen 专用 Token 算法集成**:
  - 引入 Qwen-VL 官方推荐的 Token 估算公式，基于 `smart_nframes` (智能抽帧) 与 `smart_resize` (动态分辨率) 对视频消耗进行精准预估。
  - 解决了原生 VLM 接口不返回 usage 数据导致的成本核算空白问题。

### 🎨 UI/UX 深度体验优化 (UI/UX Hardening)
- **任务创建流严格校验**: 强化了任务名称、视频上传及模型选择的联动校验，确保分析链路完整性。
- **动态结果展示适配**: 在视频识别任务中自动隐藏冗余的“环境要素抽取 (Keyframes)”区域，使报告界面更加聚焦。
- **1080P 元数据保护**: 修正了规格覆盖逻辑，确保原始高分辨率视频的元数据在经过预处理后依然能被正确展示在“基本信息”中。

---

## [v1.3.1] - 2026-05-11

### 🛠️ 数据展示与识别精准化 (Data Accuracy & Recognition)
- **Token 审计深度校准**: 
  - 修正了 MiniMax Native VLM 接口的响应解析逻辑，实现了从响应根节点精准提取 `usage` 数据。
  - 引入 **智能 Token 估算器**: 针对不返回 Token 统计的特定 VLM 接口，基于提示词长度与图像规模自动计算预估值，消除“0 Token”展示。
  - **跨表数据一致性**: 强化了 `Task` 与 `TaskResult` 表的同步机制，确保详情页数据与后台审计数据实时对齐。
- **识别模式动态适配**: 
  - 修复了识别方式标签硬编码的问题。系统现在能根据任务配置自动切换并展示 **“图像 LLM 识别”** 或 **“视频 LLM 识别”**，并匹配相应的 UI 图标。
- **元数据负载透明化**: 
  - **分辨率真实还原**: 修正了元数据提取逻辑，详情页现在展示的是 **AI 实际分析时使用的分辨率**（如经过 360p 压缩后的规格），而非仅仅是原始素材规格。
  - **Payload 规模统计**: 重新定义了“文件大小”字段，现在展示的是 **提交给 AI 的实际数据量**（所有关键帧总和），帮助用户直观理解分析规模。

### 🛡️ 系统鲁棒性与本地化 (System & Localization)
- **时区偏差修正 (Local Timezone)**: 
  - 彻底解决了任务创建/完成时间与服务器时间相差 8 小时的 UTC 偏差问题。
  - 后端全面切换至 **服务器本地时间戳** 存储，确保任务流水线时间与用户实际操作时间完美契合。
- **抽帧方案健壮性优化**:
  - **自适应回退分辨率**: 修正了 frame-extractor 的回退机制，确保在主抽帧链路失效触发回退方案时，生成的兜底帧依然严格遵循用户设定的分辨率参数。
- **路径兼容性增强**: 优化了 Windows 环境下的跨目录文件读取稳定性，解决了部分路径下 `cv2` 无法识别相对路径导致的规格读取失败。

---


## [v1.3.0] - 2026-05-11

### 📊 高保真 PDF 报表导出优化 (High-Fidelity PDF Export)
- **渲染引擎重构**: 
  - 采用 **语义化元素定位 (Semantic Targeting)** 技术，彻底解决导出 PDF 时出现的布局错位与内容截断问题。
  - 引入 **动态高度计算** 逻辑，确保长报表在跨页打印时能够精准填充，消除异常空白区域。
- **布局稳定性增强**: 
  - 固定报表容器比例，确保“基本信息”模块在导出过程中保持 1:1 视觉还原，不被主内容挤压。
  - 优化了 Echarts 维图在 PDF 打印流中的渲染稳定性，解决了部分浏览器下截图不全的难题。

### 🚀 平台启动与链路优化 (Platform & Connectivity)
- **自愈式启动验证**: 优化了前端对后端 API 的健康检查机制，在服务重启期间提供更平滑的加载过渡。
- **依赖依赖清理**: 移除了部分冗余的开发依赖，提升了前端构建速度。

---

## [v1.2.2] - 2026-05-09

### 🛡️ AI 响应鲁棒性加固 (AI Response Robustness)
- **深度 JSON 提取逻辑**: 
  - 引入正则匹配技术，支持从包含 `<think>` 思考过程或杂乱文字的 AI 回复中精准提取 JSON 数据块。
  - 即使 AI 未使用 Markdown 代码块包裹，系统也能通过 `{...}` 特征定位结论。
- **MiniMax 适配终极方案**: 
  - 自动识别 MiniMax 提供商并切换至 **混合消息格式** (Content String + Top-level Images)，解决部分版本无法识别 `image_url` 的难题。
  - 强制注入 `max_tokens: 1024`，防止长分析结论被截断导致的解析失败。
- **多端点响应兼容**: 增强了对原生 VLM 响应结构的适配，支持 `choices`、`content`、`output` 等多种返回字段。

### 🛠️ 运维与数据库优化 (Maintenance)
- **端点自动迁移**: 提供自动化脚本，将旧有的 MiniMax 基础路径一键平滑迁移至验证成功的专用 VLM 路径。
- **验证机制完善**: 新增后端本地验证脚本 `verify_minimax_fix.py`，支持脱离前端直接测试 AI 链路。

---

## [v1.2.1] - 2026-05-09

### 🚀 MiniMax 2.7 集成专项优化 (MiniMax Hardening)
- **原生 VLM 链路切换**: 
  - 彻底解决 MiniMax 2.7 在 OpenAI 兼容接口下无法识别图片的问题，成功切换至其原生 **Coding Plan VLM** 接口。
  - 引入 **图像矩阵缝合 (Image Stitching)** 技术：将视频 A 和 B 的多组关键帧垂直缝合成对比大图，适配单图识别接口，提升比对精度。
- **抽帧流程健壮性增强**:
  - **智能感知抽帧 (Perceptual Sampling)**: 优化了基于光流与场景检测的采样算法，提高运动捕捉灵敏度。
  - **首帧兜底机制**: 新增首帧强制捕获逻辑，确保即便在极端静态场景下也能提供有效的环境因子图片。

### 🎨 结果详情页精密还原 (UI/UX Restoration)
- **三栏式布局回归**: 1:1 还原最初的高阶详情页设计，重新启用 **Scoring | Content | Basics** 的三栏分块。
- **数据字段补完**: 修复并恢复了曾被移除的“任务 ID”、“创建/完成时间”、“视频详细规格（FPS/分辨率/大小）”以及“Token 审计数据”。
- **雷达图数据对齐**: 建立了 AI 维度评分与前端 Echarts 维度的自动映射机制，解决评分分布图表显示空白的问题。

### 🛠️ 后端核心逻辑优化 (Backend Fixes)
- **API 稳定性加固**: 解决了 MiniMax 接口由于 Base64 过大导致的传输失败，引入了 512px 动态图像压缩技术。
- **ImportError 修复**: 解决了后端重构过程中遗漏的 `get_video_metadata` 导入错误。

---

## [v1.2.0] - 2026-05-08

### 🚀 核心功能升级 (Core Features)
- **智能模型资产中心**:
  - 全新设计的模型管理界面，支持 **Gemini 2.0 Pro**, **GPT-4o**, **Qwen-VL-Max**, **DeepSeek**, **MiniMax-M2.7** 等主流大模型的一键快速预设。
  - 引入 **拖拽排序 (Drag & Drop)** 功能，允许用户直观调整模型的优先级顺序，并实时同步至后端。
  - 增强模型属性配置，支持细化标注模型对“文本/图片/视频”等不同模态的处理能力。
- **提示词模版库 (Prompt Library)**:
  - 建立中心化提示词管理系统，支持模版的分组、搜索与快速复用。
  - 集成高保真 Markdown 编辑器，优化了环境对比指令的编写体验，提升了 AI 分析的一致性。

### 🎨 交互与 UI 优化 (UI/UX Improvements)
- **极简主义美学重塑**:
  - 引入 **高级深色模式 (Premium Dark Mode)** 搭配 Glassmorphism 毛玻璃效果，提升平台专业感。
  - 动效增强：模态框支持 Fade-in/Zoom-in 进入动画，列表操作具备更丝滑的交互反馈。
- **菜单结构精简**:
  - 移除“结果中心”，将其功能完整并入“结果报表”，导航栏更加聚焦，降低用户认知负担。
- **响应式布局优化**: 
  - 改进了模型卡片与提示词列表在多分辨率下的适配表现。

### 🛠️ 稳定性与架构优化 (Stability & Refactoring)
- **前端代码深度重构**: 
  - 彻底修复了由于 JSX 标签嵌套错误导致的编译崩溃问题。
  - 优化了组件解耦，解决了 `ModelConfig` 与 `PromptConfig` 之间的逻辑冲突。
- **API 通讯优化**:
  - 统一了前端与 FastAPI 后端的 CRUD 交互逻辑，增强了数据持久化的鲁棒性。

### 🔧 错误修复 (Bug Fixes)
- 修复了模型编辑弹窗在特定分辨率下内容溢出无法滚动的 CSS 缺陷。
- 解决了 HMR (热更新) 期间可能导致的状态丢失问题。
- 修复了侧边栏图标对齐不统一的问题。

---

## [v1.1.0] - 2026-05-07

### 🚀 核心功能升级 (Core Features)
- **多模态模型原生视频支持**: 
  - 针对 **Qwen3-VL-Plus** 等大模型实现了原生视频直传分析，彻底废弃传统的 FFmpeg 抽帧逻辑，利用模型原生的时序理解能力提升分析准确度。
  - 优化了 **Gemini 1.5 Pro** 的视频上传流程，支持完整的视频环境感知。
- **Token 消耗统计**: 
  - 实现了端到端的 Token 审计功能。系统自动记录每次 AI 分析产生的 **输入 Token** 和 **输出 Token**，并持久化到数据库。
- **视频在线回放**: 
  - 结果详情页集成原生 HTML5 播放器，支持直接回放测试素材 A 和 B，方便人工核对 AI 分析结论。

### 🎨 交互与 UI 优化 (UI/UX Improvements)
- **任务创建流校验**: 
  - 强化了“新建任务分析流”输入层的约束：任务名称、视频 A、视频 B 现在为**强制必填项**，确保分析流的稳健性。
- **任务管理效率**: 
  - 新增**状态页签过滤**（处理中、已完成、失败），支持快速定位任务状态。
  - 新增**手动刷新按钮**（带 Spin 动画），提升列表数据同步体验。
- **结果详情增强**: 
  - 新增任务**完成时间**展示。
  - 新增**任务耗时**自动计算（精确到秒）。
- **菜单精简**: 
  - 移除“相似度报告”与“系统设置”冗余模块，聚焦核心业务流程。

### 🛠️ 架构与后端优化 (Backend & Architecture)
- **数据库架构升级**: 
  - 扩展了 `Task` 和 `TaskResult` 表结构，支持存储 Token 数据及高精度时间戳。
  - 编写并集成了数据库迁移脚本 (`migrate_db.py`)，支持无损升级。
- **模型管理逻辑**: 
  - 强制执行**单一默认模型**逻辑：在设定新默认模型时，系统自动取消旧模型的默认状态，避免路由冲突。
- **静态资源服务**: 
  - 后端集成 FastAPI `StaticFiles` 模块，高效挂载本地 `storage` 目录供前端流式加载视频。

### 🔧 错误修复 (Bug Fixes)
- 修复了 OpenAI 兼容接口在特定环境下 `client` 对象未初始化的问题。
- 优化了 Base64 编码大视频文件时的内存占用问题。
- 解决了因数据库字段缺失导致的“创建任务网络错误”。

---
*EnvMatch AI 团队 | 持续进化中...*
